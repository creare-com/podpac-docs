podpac\.core\.compositor\.Compositor
====================================

.. currentmodule:: podpac.core.compositor

.. autoclass:: Compositor

   
   .. automethod:: __init__

   
   .. rubric:: Methods

   .. autosummary::
   
      ~Compositor.__init__
      ~Compositor.add_traits
      ~Compositor.cache_obj
      ~Compositor.cache_path
      ~Compositor.class_own_trait_events
      ~Compositor.class_own_traits
      ~Compositor.class_trait_names
      ~Compositor.class_traits
      ~Compositor.clear_disk_cache
      ~Compositor.composite
      ~Compositor.copy_output_array
      ~Compositor.execute
      ~Compositor.get_hash
      ~Compositor.get_image
      ~Compositor.get_native_coordinates
      ~Compositor.get_output_coords
      ~Compositor.get_output_path
      ~Compositor.get_shared_coordinates
      ~Compositor.get_source_coordinates
      ~Compositor.has_trait
      ~Compositor.hold_trait_notifications
      ~Compositor.init
      ~Compositor.initialize_array
      ~Compositor.initialize_coord_array
      ~Compositor.initialize_output_array
      ~Compositor.iteroutputs
      ~Compositor.load
      ~Compositor.load_cached_obj
      ~Compositor.load_from_file
      ~Compositor.notify_change
      ~Compositor.observe
      ~Compositor.on_trait_change
      ~Compositor.plot
      ~Compositor.set_trait
      ~Compositor.setup_instance
      ~Compositor.trait_events
      ~Compositor.trait_metadata
      ~Compositor.trait_names
      ~Compositor.traits
      ~Compositor.unobserve
      ~Compositor.unobserve_all
      ~Compositor.write
   
   

   
   
   